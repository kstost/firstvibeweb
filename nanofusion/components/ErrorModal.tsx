import React from 'react';

// This component is not currently used in the application.
// It is populated with valid content to prevent build errors caused by an empty module file.
export const ErrorModal: React.FC = () => {
  return null;
};
